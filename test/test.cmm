extern int printInt(int x);
extern int inputInt();


int fib(int x){
    if x > 1 then{
        return fib(x - 1) + fib(x - 2);
    }

    return 1;
}

main(){
    int i:0;
    int a[10];
    while i < 10 do{
        a[i] = i;
        i = i + 1;
    }

    while i >= 0 do{
        printInt(a[i]);
        i = i - 1;
    }
}